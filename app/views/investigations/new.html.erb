<% content_for :title, "New Investigation — APRS" %>

<div class="space-y-6">
  <%= link_to investigations_path, class: "inline-flex items-center gap-1.5 text-sm text-aprs-text-muted hover:text-aprs-primary transition-colors" do %>
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
    </svg>
    Back to Investigations
  <% end %>

  <h1 class="text-2xl font-bold">New Investigation</h1>

  <%= render "form", investigation: @investigation %>
</div>
