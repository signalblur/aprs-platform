<% content_for :title, "Edit Membership — #{@membership.user.email} — APRS" %>

<div class="space-y-6">
  <h1 class="text-2xl font-bold">Edit Membership for <%= @membership.user.email %></h1>
  <p class="text-sm text-aprs-text-muted">To change the tier, create a new membership instead. This preserves the audit trail.</p>

  <div class="bg-aprs-surface rounded-xl border border-aprs-border/30 p-6">
    <%= render "form", membership: @membership,
                       form_url: membership_path(@membership),
                       show_tier_field: false %>
  </div>
</div>
